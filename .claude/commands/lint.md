Run linting and formatting checks:

1. Run ESLint:
```bash
npm run lint
```

2. Run Prettier check:
```bash
npx prettier --check .
```

If there are fixable issues, ask whether to auto-fix with `npm run lint` (already includes --fix) and `npm run format`.
