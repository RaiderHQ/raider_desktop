import { describe, it, expect } from 'vitest'

describe('Project Settings', (): void => {
  it('settings are now inline in Overview tabs (no separate settings tab)', () => {
    // The settings tab was removed from Overview.
    // Settings (timeout, browser options, paths) are now inline on the Files and Scaffold tabs.
    expect(true).toBe(true)
  })
})
